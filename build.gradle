// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.support:appcompat-v7:19.0.+'
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}
